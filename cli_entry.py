# cli_entry.py

from src.cli import cli

if __name__ == '__main__':
    cli()
# This script serves as the entry point for the CLI application.
# It imports the `cli` function from the `src.cli` module and executes it when run as a script.
# This allows users to interact with the cryptocurrency price tracker via command line. 